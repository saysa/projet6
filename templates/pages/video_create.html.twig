{% extends 'layout.html.twig' %}

{% block title %}Ajout d'une video{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row justify-content-center">
            {{ form_start(form) }}
                {{ form_rest(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
